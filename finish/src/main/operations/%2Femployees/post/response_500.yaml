---
version: "1.2"
mappings:
- body:
    mappings:
    - message:
        required: false
        nullable: false
        template: "{{$exists($error.message)? $error.message : \"Internal Server Error\"\
          }}"
